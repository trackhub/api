type RootQuery {
  tracks(limit: Int!): [Track]

  tracksMap(
    neLat: Float!,
    swLat: Float!,
    neLon: Float!,
    swLon: Float,
    skipTracks: [String!]!
  ): [Track]

  places(limit: Int): [Place]
}
